% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineage.R
\name{lineage_edge}
\alias{lineage_edge}
\title{Define a lineage edge}
\usage{
lineage_edge(
  from,
  to,
  from_type = NULL,
  to_type = NULL,
  relationship = "derives",
  condition = NA_character_,
  label = NA_character_
)
}
\arguments{
\item{from, to}{Character node identifiers. \code{to} depends on \code{from}.}

\item{from_type, to_type}{Optional node types (\code{"dataset"}, \code{"variable"},
\code{"analysis"}, \code{"output"}). Inferred when \code{NULL}.}

\item{relationship}{Edge semantics. One of \code{"derives"}, \code{"groups_by"},
\code{"filters"}, \code{"summarises"}, \code{"models"}, \code{"reports"}, \code{"transports"} or a
custom string.}

\item{condition}{Optional condition restricting when the edge applies (for
example \code{"TRT01P == 'Drug A'"}). Recorded for transparency; not evaluated.}

\item{label}{Optional human-readable label.}
}
\value{
A tibble with one row and class \code{td_edge}.
}
\description{
A lineage edge states that \code{to} depends on \code{from}. Node identifiers use a
simple convention:
}
\details{
\itemize{
\item a dataset is written as \code{"ADSL"};
\item a dataset variable is written as \code{"ADSL.TRT01P"};
\item an analysis is written as \code{"MMRM"};
\item an output (TLF) is written as \code{"Table_14_2_1"}.
}
}
