% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_cut.R
\name{compare_cut}
\alias{compare_cut}
\title{Compare two data cuts of a clinical dataset}
\usage{
compare_cut(
  old,
  new,
  by,
  compare_labels = TRUE,
  compare_types = TRUE,
  tolerance = 1e-09,
  ignore_vars = NULL,
  name_old = "old",
  name_new = "new",
  dataset = NULL,
  subject_var = NULL,
  backend = c("trialdiff", "waldo"),
  ...
)
}
\arguments{
\item{old, new}{Data frames (or tibbles) representing the earlier and later
data cut. \code{new} is compared against \code{old}.}

\item{by}{Character vector of key variables that uniquely identify an
observation within each dataset. Common clinical keys include
\code{c("USUBJID", "PARAMCD", "AVISIT")} for \code{ADLB} or \code{c("USUBJID", "VISIT")}
for \code{ADVS}. Must be present in both datasets.}

\item{compare_labels}{Logical. If \code{TRUE} (default) variable label changes are
recorded in the schema comparison.}

\item{compare_types}{Logical. If \code{TRUE} (default) variable type changes are
recorded in the schema comparison.}

\item{tolerance}{Numeric tolerance used when comparing numeric values. Set to
\code{0} for exact comparison.}

\item{ignore_vars}{Character vector of variables to exclude from the value
comparison (for example technical or audit columns).}

\item{name_old, name_new}{Character labels used in reports.}

\item{dataset}{Optional dataset name (for example \code{"ADSL"}). Inferred from
the \code{old}/\code{new} expressions when not supplied.}

\item{subject_var}{Optional subject identifier variable. When \code{NULL} the
function looks for \code{USUBJID}, then falls back to the first key.}

\item{backend}{Low-level comparison backend. \code{"trialdiff"} uses the built-in
engine; \code{"waldo"} additionally uses \pkg{waldo} for whole-column equality
short-circuiting when it is installed.}

\item{...}{Reserved for future extensions.}
}
\value{
An object of class \code{tdiff}: a list with elements \code{meta}, \code{added},
\code{removed}, \code{modified}, \code{schema}, \code{summary} and \code{register}. See
\link{tdiff-object} for details.
}
\description{
\code{compare_cut()} performs a deterministic, key-based comparison of two
versions of the same clinical dataset (for example \code{ADSL} at two data cuts).
It reports observations added, removed and modified, and separately reports
schema-level changes (variables added/removed, type and label changes).
}
\details{
The function is deliberately a \emph{low-level} comparison engine. Clinical
meaning is added by \code{\link[=classify_changes]{classify_changes()}}, lineage by \code{\link[=define_lineage]{define_lineage()}} and
impact by \code{\link[=assess_impact]{assess_impact()}}.
}
\examples{
old <- data.frame(
  USUBJID = c("S1", "S2", "S3"),
  TRT01P = c("Placebo", "Drug A", "Placebo"),
  AGE = c(54, 61, 47),
  stringsAsFactors = FALSE
)
new <- old
new$TRT01P[1] <- "Drug A"
new$AGE[2] <- 62
diff <- compare_cut(old, new, by = "USUBJID", dataset = "ADSL")
diff
}
