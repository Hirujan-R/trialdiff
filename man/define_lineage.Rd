% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineage.R
\name{define_lineage}
\alias{define_lineage}
\title{Define a data lineage graph}
\usage{
define_lineage(
  ...,
  edges = NULL,
  nodes = NULL,
  dataset = NULL,
  variable = NULL,
  downstream = NULL,
  relationship = "derives"
)
}
\arguments{
\item{...}{One or more edge tibbles created by \code{\link[=lineage_edge]{lineage_edge()}}, or a single
data frame of edges.}

\item{edges}{Optional data frame of edges with columns \code{from}, \code{to} and
optionally \code{from_type}, \code{to_type}, \code{relationship}, \code{condition}.}

\item{nodes}{Optional node table with columns \code{node} and \code{type}, used to
override inferred types and to attach labels.}

\item{dataset, variable, downstream}{Convenience arguments matching the common
workflow: declare that \code{downstream} objects depend on \code{dataset.variable}.}

\item{relationship}{Relationship used with the convenience arguments.}
}
\value{
An object of class \code{td_lineage}.
}
\description{
\code{define_lineage()} collects lineage edges into an auditable graph that can be
queried with \code{\link[=trace_dependencies]{trace_dependencies()}} and used by \code{\link[=assess_impact]{assess_impact()}}. The graph
is stored as a tidy edge list plus a node table; no external graph library is
required, although \code{\link[=as_igraph]{as_igraph()}} is provided for users who prefer one.
}
\examples{
lineage <- define_lineage(
  lineage_edge("ADSL.TRT01P", "ADLB.TRT01P", relationship = "derives"),
  lineage_edge("ADLB.AVAL", "MMRM", relationship = "models"),
  lineage_edge("MMRM", "Table_14_2_1", relationship = "reports")
)
trace_dependencies(lineage, from = "ADSL.TRT01P")
}
