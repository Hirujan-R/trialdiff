% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_changes.R
\name{classify_changes}
\alias{classify_changes}
\title{Classify detected changes}
\usage{
classify_changes(x, rules = td_default_rules(), context = list(), ...)
}
\arguments{
\item{x}{A \code{tdiff} object returned by \code{\link[=compare_cut]{compare_cut()}}, or a change register
returned by \code{\link[=as_register]{as_register()}}.}

\item{rules}{A list of \code{\link[=td_rule]{td_rule()}} objects. Defaults to
\code{\link[=td_default_rules]{td_default_rules()}}.}

\item{context}{Optional named list of extra context passed to rules, for
example \code{list(has_paramcd = TRUE)}. Context is merged with context derived
from \code{x}.}

\item{...}{Unused.}
}
\value{
When \code{x} is a \code{tdiff}, the same object with classification columns
added to \verb{$added}, \verb{$removed}, \verb{$modified} and \verb{$schema}, a \verb{$register}
element, and class \code{c("tdiff_classified", "tdiff")}. When \code{x} is a
register (data frame), a classified tibble.
}
\description{
\code{classify_changes()} turns the raw output of \code{\link[=compare_cut]{compare_cut()}} into a
clinically meaningful change register. Every change is assigned a
transparent, rule-based category (see \code{\link[=td_default_rules]{td_default_rules()}}) together with a
human-readable explanation.
}
\examples{
old <- data.frame(USUBJID = c("S1", "S2"), TRT01P = c("Placebo", "Drug A"))
new <- data.frame(USUBJID = c("S1", "S2"), TRT01P = c("Drug A", "Drug A"))
compare_cut(old, new, by = "USUBJID", dataset = "ADSL") |>
  classify_changes()
}
