% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impact.R
\name{assess_impact}
\alias{assess_impact}
\title{Assess downstream impact of classified changes}
\usage{
assess_impact(changes, lineage, policy = impact_policy(), ...)
}
\arguments{
\item{changes}{A classified \code{tdiff} object (output of \code{\link[=classify_changes]{classify_changes()}})
or a change register with a \code{category} column.}

\item{lineage}{A \code{td_lineage} object from \code{\link[=define_lineage]{define_lineage()}}.}

\item{policy}{An \code{\link[=impact_policy]{impact_policy()}} object.}

\item{...}{Reserved for future extensions.}
}
\value{
An object of class \code{td_impact} with elements \code{impacts}, \code{by_subject},
\code{unlinked}, \code{summary}, \code{policy} and \code{meta}.
}
\description{
Combines a classified change set with a lineage graph to identify which
downstream variables, analyses and outputs \emph{could} be affected. Impact is
graded as \code{"definitely_affected"}, \code{"potentially_affected"} or \code{"unlikely"},
with an explicit rationale and the triggering change identifiers.
}
\details{
\code{trialdiff} never claims statistical impact. Every affected analysis or
output is reported as requiring programmer/statistician review and, where
appropriate, a rerun.
}
\examples{
old <- data.frame(USUBJID = c("S1", "S2"), TRT01P = c("Placebo", "Drug A"))
new <- data.frame(USUBJID = c("S1", "S2"), TRT01P = c("Drug A", "Drug A"))
diff <- compare_cut(old, new, by = "USUBJID", dataset = "ADSL") |>
  classify_changes()
lineage <- define_lineage(
  lineage_edge("ADSL.TRT01P", "ADLB.TRT01P", relationship = "groups_by"),
  lineage_edge("ADLB.TRT01P", "MMRM", relationship = "models"),
  lineage_edge("MMRM", "Table_14_2_1", relationship = "reports")
)
assess_impact(diff, lineage)
}
