% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata-lineage.R
\name{lineage_from_metadata}
\alias{lineage_from_metadata}
\title{Derive lineage from study metadata}
\usage{
lineage_from_metadata(
  metadata,
  include_where = TRUE,
  include_sdtm = TRUE,
  aliases = NULL,
  overrides = NULL,
  ...
)
}
\arguments{
\item{metadata}{A \code{metacore} object (from \pkg{metacore}) or a list with
elements \code{ds_vars}, \code{ds_spec}, \code{value_spec} and \code{derivations}.}

\item{include_where}{Logical. If \code{TRUE} (default), variables referenced in
\code{value_spec$where} clauses are added as \code{"filters"} edges.}

\item{include_sdtm}{Logical. If \code{TRUE} (default), \code{DATASET.VARIABLE}
references to non-ADaM domains (for example \code{DM.ARM}) are kept as source
nodes.}

\item{aliases}{Optional data frame with columns \code{token} and \code{node}, and an
optional \code{dataset} column to scope the alias to one dataset. Aliases
resolve tokens that would otherwise be ambiguous or unknown.}

\item{overrides}{Optional edge table (for example from \code{\link[=output_registry]{output_registry()}})
merged into the generated graph with \code{\link[=add_edges]{add_edges()}}.}

\item{...}{Reserved for future extensions.}
}
\value{
A \code{td_lineage} object with additional elements \code{review} (references
needing manual attention) and \code{provenance}.
}
\description{
\code{lineage_from_metadata()} builds a \code{\link[=define_lineage]{define_lineage()}} graph from a
\pkg{metacore} object (or any list with the same tables). Dataset and
variable nodes come from \code{ds_vars}/\code{ds_spec}, and dependency edges are parsed
from the \code{derivations} and \code{where} columns of \code{value_spec}.
}
\details{
Parsing is deterministic and conservative: only tokens that resolve to known
variables (or explicit \code{DATASET.VARIABLE} references) become edges. Every
edge records its provenance, and references that cannot be resolved uniquely
are returned in a review table rather than guessed. See \code{\link[=lineage_review]{lineage_review()}}.
}
\examples{
metadata <- list(
  ds_spec = data.frame(dataset = c("ADSL", "ADLB"), stringsAsFactors = FALSE),
  ds_vars = data.frame(
    dataset = c("ADSL", "ADSL", "ADLB", "ADLB", "ADLB"),
    variable = c("USUBJID", "TRT01P", "USUBJID", "AVAL", "CHG"),
    stringsAsFactors = FALSE
  ),
  value_spec = data.frame(
    dataset = c("ADSL", "ADLB", "ADLB"),
    variable = c("TRT01P", "AVAL", "CHG"),
    derivation_id = c("MT.ADSL.TRT01P", "MT.ADLB.AVAL", "MT.ADLB.CHG"),
    where = c(NA, "PARAMCD == 'ALT'", NA),
    stringsAsFactors = FALSE
  ),
  derivations = data.frame(
    derivation_id = c("MT.ADSL.TRT01P", "MT.ADLB.AVAL", "MT.ADLB.CHG"),
    derivation = c("DM.ARM", "ADSL.TRT01P", "AVAL - BASE"),
    stringsAsFactors = FALSE
  )
)
lineage_from_metadata(metadata)
}
